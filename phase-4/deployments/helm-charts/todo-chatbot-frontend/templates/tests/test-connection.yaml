apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "todo-chatbot-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "todo-chatbot-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "todo-chatbot-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
